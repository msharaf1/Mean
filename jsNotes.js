function nums()[

]


for(let item in nums)
console.log(nums[item])


for(let item of nums){
    cosnole.log(nums[num]);
}

for(let num of nums){
    cosnole.log(num); // this will only log the value. if you don't want the index, use this.
}